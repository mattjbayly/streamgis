% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_upstream_downstream_segments.R
\name{get_upstream_downstream_segments}
\alias{get_upstream_downstream_segments}
\title{Get Upstream or Downstream Stream Segments}
\usage{
get_upstream_downstream_segments(
  streamlines,
  target,
  direction = c("downstream", "upstream"),
  reverse_direction = FALSE
)
}
\arguments{
\item{streamlines}{An \code{sf} object with LINESTRING or MULTILINESTRING geometry
representing the stream network.}

\item{target}{Either:
\itemize{
\item An integer specifying the row index of the target segment in \code{streamlines}
\item An \code{sf} POINT object that will be snapped to the nearest segment
}}

\item{direction}{Character. Either \code{"upstream"} or \code{"downstream"} to specify
which connected segments to return. Default is \code{"downstream"}.}

\item{reverse_direction}{Logical. If \code{TRUE}, reverses the assumed flow direction.
Use this if your lines are digitized downstream-to-upstream (opposite of the
typical convention). Default is \code{FALSE}.}
}
\value{
An \code{sf} LINESTRING object containing the stream segments that are
upstream or downstream of the target segment. The target segment itself is
NOT included in the output. Returns an empty sf object (0 rows) if no
connected segments are found in the specified direction.

All original attributes from \code{streamlines} are preserved.
}
\description{
Selects stream segments that are upstream or downstream of a
target segment using network traversal. Directionality is determined by the
underlying line geometry direction (start vertex to end vertex).
}
\details{
This function performs the following steps:
\enumerate{
\item Creates a directed network from streamlines using \code{sfnetworks}
\item Identifies the target segment (by index or spatial query)
\item Uses graph traversal to find connected upstream or downstream segments
\item Returns the subset of streamlines matching the traversal
}

The direction of flow is inferred from line geometry:
\itemize{
\item By default, assumes lines are digitized in the direction of flow
(upstream vertex to downstream vertex, which is common for hydrological data)
\item For downstream: traverses from the target's end node following edge direction
\item For upstream: traverses to the target's start node against edge direction
}

If your data is digitized downstream-to-upstream, set \code{reverse_direction = TRUE}.
}
\examples{
\dontrun{
library(sf)

# Load stream network
fname <- system.file("extdata", "bcfwa2.gpkg", package="streamgis")
bcfwa <- sf::st_read(fname)

# Get downstream segments from segment 5
downstream <- get_upstream_downstream_segments(streams, target = 5, direction = "downstream")

# Get upstream segments using a point location
point <- st_sfc(st_point(c(500000, 5000000)), crs = st_crs(streams))
point_sf <- st_sf(geometry = point)
upstream <- get_upstream_downstream_segments(streams, target = point_sf, direction = "upstream")

# Plot results
plot(st_geometry(streams), col = "grey")
plot(st_geometry(downstream), col = "blue", lwd = 2, add = TRUE)
}

}
